[project]
name = "ebnn"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bayesmates",
    "ipykernel>=7.1.0",
    "plotnine>=0.15.1",
]

[tool.uv.sources]
bayesmates = { git = "https://github.com/EmanuelSommer/bayesmates.git" }
